---@meta awful.prompt

---@class awful.prompt
local M

function M.run(args, textbox, exe_callback, completion_callback, history_path, history_max, done_callback, changed_callback, keypressed_callback) end

return M
